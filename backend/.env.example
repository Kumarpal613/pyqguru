# Paste your MongoDB Atlas connection string here. Do not commit the real .env file.
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/?retryWrites=true&w=majority
MONGODB_DB=pyqguru
# Optional when deploying the frontend separately
FRONTEND_URL=http://localhost:5173
